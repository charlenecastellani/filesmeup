<!---- appel sidebar --->
<%= render "shared/sidebar" %>


<div class="navbar-user">
  <div class="header">
    <h5><%= "Bonjour #{current_user.prenom} #{current_user.nom}" %></h5>
    <h1>DASHBOARD</h1>
    <li class="nav-item dropdown">
      <%= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <a><%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "dropdown-item"%></a>
      </div>
    </li>
  </div>
</div>










<!---- BLOCK DEBUT TEST --->

<div class="index-container">
  <div class="mother-container-chiffres">
    <div class="chiffres">
      <h2>24</h2>
      <p>Documents envoyés</p>
    </div>

    <div class="chiffres">
      <div class="chiffres-2">
        <h2>48</h2>
      </div>
      <p>Documents reçus </p>
    </div>

    <div class="chiffres">
      <div class="chiffres-3">
        <h2>12</h2>
        <p>Dossiers clos</p>
      </div>
    </div>
  </div>

   <body>
        <div class="container-btn">
            <button class="btn btn1">En cours</button>
        </div>
    </body>
</html>

  <div class="container-2">
    <div class="demandes">
      <p>Demandes faites</p>
      <% @emetteur_requests.each do |request| %>
        <%= link_to request_path(request), class: "remove_link_style_index" do  %>
          <div class="card-demandes">
              <!--- ligne 1 --->

              <div class="index-user-info">
                <%= cl_image_tag current_user.photo.key, class: "avatar" %>
                <div class="info">
                  <strong><p><%= "#{request.destinataire.prenom} #{request.destinataire.nom}"  %></p></strong>
                  <p><%=request.destinataire.entreprise %></p>
                </div>
              </div>
                    <!--- ligne 2 --->
              <div class="index-deadline">
                <div class="dealine-h4">
                  <h4><%=request.date_echeance.strftime("%d/%m/%Y") %></h4>
                </div>
              </div>


                     <p>Pourcentage <%= request.avancement%> %</p>
               <!--- ligne 3 --->
               <div class="validation">
                  <div class="dealine-h4">
                    <% if @doc_a_valider > 0  %>
                        <p class="warning">
                          <i class="fas fa-exclamation-triangle"></i>
                        </p>
                    <% end %>
                  </div>
                </div>
                 <!--- ligne 3 --->
              <div class="index-avancement">
                <strong><p>avancement </strong><%= request.avancement%> %</p>
              </div>
                 <!--- ligne 4
               <div class="ligne4-nouveau-document">
                 <p> nouveau document <%= @doc_a_valider%></p>
               </div>
               --->
              </div>
            <% end %>
        <% end %>
      </div>


        <div class="demandes"><p>Demandes reçues</p>
          <% @destinataire_requests.each do |request| %>
            <%= link_to request_path(request), class: "remove_link_style" do  %>

                <div class="card-demandes">
                  <div class="index-user-info">
                      <div class="info">
                        <p><%=request.titre %></p>
                      </div>
                   </div>

                    <p><%=request.emetteur.entreprise %></p>
                    <p><%=request.date_echeance.strftime("%d/%m/%Y") %></p>
                    <p>avancement : <%= request.avancement%> %</p>
                    <p>document refusé <%= @doc_refuse%> </p>
                  </div>

              <% end %>

          <% end %>
        </div>
      </div>
  </div>
</div>
